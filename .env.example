# ── Common App Config ───────────────────────────────────────────────
# Environment name: development | staging | production
NODE_ENV=development

# Global org demo key (used in smoke/tests)
ORG=demo

# Default base URLs
BASE=http://127.0.0.1:4000

# ── Auth / Tokens ───────────────────────────────────────────────────
# Stable local test account (replace in prod)
API_EMAIL=tester@example.com
API_PASS=password

# JWT secret (replace in production!)
JWT_SECRET=super-secret-change-me

# ── Database (shared by Prisma) ─────────────────────────────────────
# Example: PostgreSQL (change per environment)
DATABASE_URL="postgresql://user:pass@localhost:5432/social_business_os"

# ── Queue / Worker ──────────────────────────────────────────────────
# BullMQ / Redis URL
REDIS_URL=redis://localhost:6379

# ── Telemetry / Observability ───────────────────────────────────────
SENTRY_DSN=
LOG_LEVEL=debug