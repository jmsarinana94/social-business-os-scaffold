{
  "name": "social-business-os-scaffold",
  "private": true,
  "packageManager": "pnpm@9.12.2",
  "scripts": {
    "db:generate": "pnpm -F @repo/api prisma generate",
    "db:push": "pnpm -F @repo/api prisma db push",
    "seed": "pnpm -F @repo/api seed",
    "test:e2e": "pnpm -F @repo/api test:e2e"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "prisma": "^6.14.0"
  },
  "dependencies": {
    "@prisma/client": "^6.14.0"
  }
}