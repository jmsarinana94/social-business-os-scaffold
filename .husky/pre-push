#!/usr/bin/env sh
set -euo pipefail

echo "🚀 Running pre-push checks (unit tests only)..."

# Run only unit tests under test/unit, allow zero tests without failing
pnpm -C apps/api exec jest --config jest.config.cjs --passWithNoTests --testPathPattern '^test/unit/.*\.spec\.ts$'

echo "✅ Pre-push checks passed!"