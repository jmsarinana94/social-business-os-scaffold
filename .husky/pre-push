#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"
set -euo pipefail

echo "🚀 Running pre-push checks..."

# Run API tests but don't fail if a package currently has zero tests
pnpm -C apps/api exec jest --config jest.config.cjs --passWithNoTests

echo "✅ Pre-push checks passed!"