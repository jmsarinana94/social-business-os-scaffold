# =====================================
# 🌐 API Environment Example (apps/api)
# =====================================

# -------------------------------------
# 🧭 App Configuration
# -------------------------------------
NODE_ENV=development
PORT=4000
ORG=demo
API_BASE=http://127.0.0.1:4000

# -------------------------------------
# 🔐 Authentication & Security
# -------------------------------------
# JWT secret key used for signing tokens
JWT_SECRET=changeme-super-secret
# Token expiration time (e.g. "7d", "12h", "1h")
JWT_EXPIRES_IN=7d
# Number of bcrypt salt rounds for password hashing
BCRYPT_SALT_ROUNDS=10

# -------------------------------------
# 👤 Seed / Demo Defaults
# -------------------------------------
# Used by prisma/seed.ts and smoke.sh for demo setup
API_EMAIL=tester@example.com
API_PASS=password123
SEED_EMAIL=${API_EMAIL}
SEED_PASSWORD=${API_PASS}
SEED_ORG_SLUG=demo-9bsfct
SEED_ORG_NAME=Demo Org

# -------------------------------------
# 🗄️ Database
# -------------------------------------
# Default: PostgreSQL for development
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/sbo?schema=public

# Alternate: SQLite (used for lightweight local or CI)
# DATABASE_URL="file:./prisma/dev.db"

# -------------------------------------
# ⚙️ Optional: Redis (for BullMQ / caching)
# -------------------------------------
# REDIS_URL=redis://localhost:6379

# -------------------------------------
# 💳 Optional: Stripe Integration
# -------------------------------------
# STRIPE_SECRET_KEY=sk_test_xxx
# STRIPE_WEBHOOK_SECRET=whsec_xxx

# -------------------------------------
# 🧪 Test / CI Overrides
# -------------------------------------
# DATABASE_URL="file:./prisma/test.db?connection_limit=1&socket_timeout=5"
# NODE_ENV=test