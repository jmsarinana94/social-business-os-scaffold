export BASE="http://localhost:4000"
export ORG="demo"
source_env_if_exists .envrc.local

# use a stable local account so tokens don't change
export API_EMAIL="tester@example.com"
export API_PASS="password"

# load the curl helper functions (aget/apost/aput/adel, ensure_tok, etc.)
source ./scripts/api-helpers.sh
