# apps/api/.envrc — minimal

# Load local app env if present (no DATABASE_URL here)
dotenv_if_exists ".env"

# Load Prisma SQLite env (defines DATABASE_URL="file:./dev.db")
dotenv_if_exists "prisma/.env"

# Safety defaults
export DATABASE_URL="${DATABASE_URL:-file:./dev.db}"
export JWT_SECRET="${JWT_SECRET:-e2e-test-secret}"
