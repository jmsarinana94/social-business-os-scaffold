# apps/api/.envrc
export BASE="http://localhost:4010/v1"
export EMAIL="tester@example.com"

# If you have an org ID, set it here (or leave blank and rely on ORG_SLUG flow)
export ORG_ID="cmg7h3o1j0000siron00aamg6"
export ORG_SLUG=""

# OPTIONAL: demo password if your login_api reads PASS
export PASS='Passw0rd!demo'

echo "✅ direnv loaded: BASE=$BASE  ORG_ID=$ORG_ID  ORG_SLUG=$ORG_SLUG  EMAIL=$EMAIL"