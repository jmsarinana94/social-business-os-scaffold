# SQLite (default for local / tests)
DATABASE_URL="file:./dev.db"

# Postgres (uncomment to switch)
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/socialos?schema=public"

# JWT for /auth
JWT_SECRET="dev_jwt_secret"