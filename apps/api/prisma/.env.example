# -----------------------------------
# 🌱 Environment Example for apps/api
# -----------------------------------

# -----------------------------------
# Database Configuration
# -----------------------------------

# Default: SQLite (used for local dev & tests)
DATABASE_URL="file:./dev.db"

# Optional: Switch to Postgres for staging/production
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/socialos?schema=public"

# -----------------------------------
# Auth / Security
# -----------------------------------
# Secret key for JWT signing (used by NestJS auth module)
JWT_SECRET="dev_jwt_secret"

# -----------------------------------
# API / Server Config
# -----------------------------------
# Default local port for NestJS API
PORT=4000

# Base URL for local smoke tests and integrations
API_BASE="http://127.0.0.1:4000"

# -----------------------------------
# Seed / E2E Defaults
# -----------------------------------
# Used by prisma/seed.ts and smoke.sh
SEED_EMAIL="tester@example.com"
SEED_PASSWORD="password123"
SEED_ORG_SLUG="demo-9bsfct"
SEED_ORG_NAME="Demo Org"

# -----------------------------------
# Optional: Redis / Queueing (for BullMQ)
# -----------------------------------
# REDIS_URL="redis://localhost:6379"

# -----------------------------------
# Optional: Stripe (for billing tests)
# -----------------------------------
# STRIPE_SECRET_KEY="sk_test_xxx"
# STRIPE_WEBHOOK_SECRET="whsec_xxx"