# Local API Demo Commands

This document explains how to use **Makefile.local** and **run_demo_once.sh**  
to run local API smoke tests and demo simulations inside the `apps/api` folder.

---

## 🧰 Prerequisites

1. Make sure your local environment variables are loaded:
   ```bash
   direnv allow