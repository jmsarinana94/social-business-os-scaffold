// Minimal no-op telemetry so builds/tests don't fail.
// Replace with your actual OTEL setup later if/when needed.

export function startOtel(): void {
  // noop
}

export async function shutdownOtel(): Promise<void> {
  // noop
}